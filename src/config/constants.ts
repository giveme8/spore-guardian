export const GAME_WIDTH = 1024;
export const GAME_HEIGHT = 640;

export const GRID_COLS = 9;
export const GRID_ROWS = 5;
export const CELL_SIZE = 88;

// Grid origin (top-left of cell [0,0])
export const GRID_ORIGIN_X = 120;
export const GRID_ORIGIN_Y = 80;

// UI strip at bottom
export const UI_HEIGHT = 110;

export const ENEMY_START_X = GAME_WIDTH + 60;
export const ENEMY_END_X = -80; // reached base

export const SUN_FALL_SPEED = 120; // px/s
export const SUN_VALUE = 25;
export const SUN_LIFETIME = 7000; // ms before despawn
